# work(一時 spec / plan 置き場)

> ここは**一時物**の置き場(この README 以外は git 管理外)。機能の実装完了時に**クローズ蒸留**(`spec-close`)して削除する。
> 恒久に残す先: 決定=`docs/adr/` / 用語=`docs/glossary.md` / 受け入れ条件=テスト名 / 現状仕様=生成+テスト。

- `SPEC-<topic>.md` — 仕様(`/spec` で草案 → 人がレビュー & 承認)
- `PLAN-<topic>.md` — 実装プラン(`/plan` でチェックリスト化。フェーズ分割し、完了ごとに `- [x]`)
