# トレーサビリティ索引

`REQ → ADR → test → code` の対応表。**`/trace` で検査・更新**する(手で厳密に保つ必要はない)。

| REQ | ADR | テスト | コード | 備考 |
|-----|-----|--------|--------|------|
| —   | ADR-0001 | — | — | まだ機能未追加。最初の REQ を追加したらここに行を足す |

検査項目の一覧は `/trace`(`.claude/commands/trace.md`)が正(ここに複製しない)。
